
<div class="d-flex justify-content-center h-100">
<%= form_with(model: comment, class: "w-25 mt-5") do |form| %>
  <% if comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
        <% comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-outline mb-2">
    <%= form.label :comment, style: "display: block", class: "form-label" %>
    <%= form.text_field :comment, class: "form-control" %>
  </div>

  <span style="opacity: 0.5">The article field is the id of the article</span>
  <div class="form-outline mb-2">
    <%= form.label :article_id, style: "display: block", class: "form-label" %>
    <%= form.text_field :article_id, class: "form-control" %>
  </div>

  <div class="row mb-4">
    <div class="d-flex justify-content-center">
      <%= form.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
</div>